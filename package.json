{
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "parcel": "^2.8.3"
  },
  "scripts": {
    "start": "parcel ./src/index.html",
    "deploy": "parcel build ./src/index.html --public-url ./ && gh-pages -d dist"
  }
}
